import React from 'react';
import ReactDOM from 'react-dom';
import SpecialCharacters from './specialCharacters';

ReactDOM.render(<SpecialCharacters />, document.getElementById('tool-root-mount') );
